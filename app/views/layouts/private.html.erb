
<%= react_component('navigation/PrivateNavbar', {userId: current_user.id}) %>

<% content_for :content do %>
  <%= content_for?(:users_content) ? yield(:users_content) : yield %>
<% end %>

<%= render template: "layouts/application" %>
